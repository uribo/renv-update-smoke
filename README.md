# renv-update-smoke

Smoke-test repository for the scheduled `renv` update workflow.

The default branch intentionally starts with an older `renv` lockfile so the
workflow can open an update pull request.
